<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>List of URLs</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css">
</head>

<body>
  <h1>Your short URLs</h1>

  <div class="container">
    <% for(const key in urls) { %>
      <div class="row"> <!-- url is an array where 0 is key and 1 is full url-->
        <div class="col-4">
          <a href="http://localhost:8080/<%= key %>" target="_blank">http://localhost:8080/<%= key %></a>
        </div>
        <div class="col-4">
          (<%= urls[key] %>)
        </div>
      </div>
      <% } %>
  </div>

</body>

</html>